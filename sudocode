{
int isBuiltin(char *cmd)
char builtinList[2][] = "exit", NULL;
int i = 0, ret = 0

for i = 0; builtinList[i]; i++
ret = _strncmp(builtinList[i], cmd, strlen(cmd))
if ret = 0 && strlen(builtinList[i]) == strlen(cmd)
executeBuiltin(builtinList[i])
return (0)

return -1
}

{
void (terminate(int status)
putchar('\n')
exit(status)
}

{
void printEnv(char **env)
int i = 0

for (i = 0; env[i]; i++)
_puts(STDOUT_FILENO, env[i])
}
